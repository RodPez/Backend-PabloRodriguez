const {Router} = require("express");
const Users = require("../dao/models/Users.model");

const router = Router();

router.post("/", (req,res) =>{
    try {
        
    } catch (error) {
        
    }
})

module.exports = router;